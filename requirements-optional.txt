# =============================================================================
# E2E Type Converter - Optional Dependencies
# =============================================================================
# 可选依赖：按功能模块分组的第三方库支持
# 用户可根据需要选择安装特定功能的依赖
# =============================================================================

# -----------------------------------------------------------------------------
# NumPy 支持
# 功能：NumPy 数组与 Python 标准类型的互转
# -----------------------------------------------------------------------------
numpy>=1.20.0

# -----------------------------------------------------------------------------
# SciPy 支持
# 功能：SciPy 稀疏矩阵的转换支持
# 依赖：numpy
# -----------------------------------------------------------------------------
scipy>=1.7.0

# -----------------------------------------------------------------------------
# Pandas 支持
# 功能：Pandas DataFrame/Series 的转换支持
# 依赖：numpy
# -----------------------------------------------------------------------------
pandas>=1.3.0

# -----------------------------------------------------------------------------
# PyTorch 支持
# 功能：PyTorch Tensor 的转换支持
# 注意：GPU 版本需要根据 CUDA 版本选择对应的 torch 版本
# -----------------------------------------------------------------------------
torch>=1.9.0

# -----------------------------------------------------------------------------
# Xarray 支持
# 功能：Xarray DataArray/Dataset 的转换支持
# 依赖：numpy
# -----------------------------------------------------------------------------
xarray>=0.19.0

# -----------------------------------------------------------------------------
# JAX 支持
# 功能：JAX Array 的转换支持
# -----------------------------------------------------------------------------
jax>=0.2.0

# -----------------------------------------------------------------------------
# TensorFlow 支持
# 功能：TensorFlow Tensor 的转换支持
# -----------------------------------------------------------------------------
tensorflow>=2.6.0

# -----------------------------------------------------------------------------
# CuPy 支持 (可选，需要 NVIDIA GPU)
# 功能：CuPy 数组的转换支持，提供 GPU 加速
# 注意：需要根据 CUDA 版本安装对应的 cupy 版本
# 安装：pip install cupy-cuda11x 或 cupy-cuda12x
# -----------------------------------------------------------------------------
# cupy>=10.0.0
